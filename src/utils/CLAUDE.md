# Utils Module

## Purpose
Shared utility functions used across multiple modules.

## Conventions
- Pure functions only — no side effects, no external dependencies
- Each utility has a single, well-defined purpose
- All utilities must have unit tests
- No business logic here — only generic helpers (formatting, validation, date utils, etc.)
