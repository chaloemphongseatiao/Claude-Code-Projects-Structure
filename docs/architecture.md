# Architecture

## Overview
Document the high-level architecture of the project here.

## Components

### API Layer (`src/api/`)
Handles external communication and request routing.

### Persistence Layer (`src/persistence/`)
Manages data storage and retrieval.

## Decisions
See `decisions/` folder for Architecture Decision Records (ADRs).
